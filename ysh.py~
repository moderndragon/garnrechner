#!/usr/bin/python
# This Python file uses the following encoding: utf-8
import os, sys 

# meters per skein in yarn a
la = int(raw_input(""))

# meters per skein of yarn b
lb = int(raw_input("Bitte gib an, wieviele Meter auf einem Knäuel des zweiten Garns sind:"))

# Anzahl der Knäuel von Garn a
x = int(raw_input("Bitte gib an, wieviele Knäuel des ersten Garns verwendet werden (ganze Zahl):"))

# Gesamtlänge
ltot = la * x

# benoetigte  von Garn b
y = x * la // lb + 1

print "Die insgesamt benötigte Garnlänge ist", ltot, "Meter."
print "Du benötigst", y, "Knäuel von Garn b."